# Plan - Fix Recommendation Variety and Handle RapidAPI 404 (27_06_2026-23_58)

We will address the duplicate recommendations issue by passing the artist name separately from the frontend and optimizing the backend YouTube search fallback. We will also document the RapidAPI 404 response details.

## Proposed Changes

### 1. Frontend Parameter Passing
Modify `src/components/TrackLibrary.tsx`:
- Pass `artist` parameter separately in `fetchRecommendations` call.

### 2. Backend Fallback Query Optimization
Modify `backend/proxy.js`:
- Retrieve `artist` in `/api/recommend`.
- If we fall back to YouTube search:
  - If `artist` is present, search for `${artist} music videos` instead of the song title query, providing a list of different popular tracks by that artist.
  - If `artist` is missing but `q` is present, search for `${q} similar songs` to find cover versions and related tracks rather than exact duplicate video uploads.

---

## Verification Plan

### Manual Verification
1. Run local dev server.
2. Load "Bamboléo" by "Gipsy Kings".
3. Open "Recs" tab and confirm it displays a variety of different Gipsy Kings songs (e.g. Volare, Djobi Djoba, Baila Me) instead of 8 duplicate uploads of Bamboléo.
