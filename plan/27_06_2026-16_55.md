# Plan - Rename SmartPanel and Increase Height (27_06_2026-16_55)

We will rename the component `SmartPanel` to `TrackLibrary` (or another user-preferred name) to align with standard DJ software terminology (e.g., Library/Browser), and we will increase its expanded height to make it more functional.

## Proposed Changes

### Renaming Component & Files
We will rename:
- `src/components/SmartPanel.tsx` ➔ [NEW] `src/components/TrackLibrary.tsx`
- [DELETE] `src/components/SmartPanel.tsx`

We will update imports and usages in:
- [MODIFY] `src/App.tsx`
  - Change import from `./components/SmartPanel` to `./components/TrackLibrary`
  - Change `<SmartPanel ... />` to `<TrackLibrary ... />`

### Renaming UI Labels
Inside the component, we will rename:
- The collapsed header label from `"Panel"` to `"Track Library"` (or `"Library"`).
- Component name from `SmartPanel` to `TrackLibrary`.

### Increasing Height
Currently, the panel has the following height-related classes:
- Expanded panel wrapper: `max-h-[60vh]`
- Expanded content wrapper: `max-h-[78vh]` (this is constrained by the parent's `max-h-[60vh]`)
- Library list container: `max-h-[40vh]`
- Search results list container: `max-h-[40vh]`

We will increase these to:
- Expanded panel wrapper: `max-h-[80vh]` (or a user-preferred height, e.g., `max-h-[85vh]`)
- Expanded content wrapper: `max-h-[calc(80vh-36px)]` (to fit properly inside the parent)
- Library list container: `max-h-[55vh]` (allowing more tracks to be visible simultaneously)
- Search results list container: `max-h-[55vh]` (allowing more search results to be visible)

---

## User Review Required
We propose renaming `SmartPanel` to `TrackLibrary` (and the file to `TrackLibrary.tsx`). The collapsed panel title text will display **"Track Library"** instead of **"Panel"**. We will increase the panel's maximum height from `60vh` to `80vh` when open. Inside, individual lists (Library and Search) will be expanded from `40vh` max-height to `55vh`.
