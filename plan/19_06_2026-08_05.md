# Plan: Review & Fix DjMixer to match Stitch Design System

## Goal
Fix compilation warning/error (unused `apiCount`) and update the colors to align with the Stitch Design System (`projects/13268962760631203709`).

## Proposed Changes

### 1. Fix Compilation Error
- **File**: `src/App.tsx`
- **Change**: Remove the unused `apiCount` variable declaration to fix `TS6133: 'apiCount' is declared but its value is never read.`

### 2. Update Colors to Match Design System
- **File**: `src/app.css`
- **Change**: Change deck A color variable `--color-deck-a` to Hot Pink `#ff0080`, deck B color variable `--color-deck-b` to Cyan `#00d4ff`. Also update `--color-bg-darkest` to `#0a0a0a` to match the Deep Space foundation.
- **File**: `src/App.tsx`
- **Change**: Update color props passed to Deck A (`color="#ff0080"`) and Deck B (`color="#00d4ff"`).
- **File**: `src/components/Mixer.tsx`
- **Change**: Update color references passed to vertical/horizontal sliders and inline EQ to `#ff0080` (Deck A) and `#00d4ff` (Deck B).
- **File**: `src/components/HorizontalSlider.tsx`
- **Change**: Update default color from `#009afe` to `#ff0080`.

## Verification
- Run `npm run build` to verify there are no compilation errors.
- Run `npm run test` to verify all tests still pass.
