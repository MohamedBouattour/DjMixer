export const sharedStyles = {
    modalOverlay: "fixed inset-0 bg-black/85 backdrop-blur-md z-[10000] flex items-center justify-center",
    modalBase: "bg-bg-panel border border-white/10 rounded-xl shadow-[0_20px_60px_rgba(0,0,0,0.8)] overflow-hidden",
    modalHeader: "flex items-center justify-between px-5 py-4 border-b border-white/10 bg-bg-header",
    iconBtnClose: "w-8 h-8 rounded-full bg-bg-control-dark border border-white/10 text-text-hint flex items-center justify-center cursor-pointer transition-all duration-150 hover:bg-accent-red hover:text-white"
};
